<h1>All Projects</h1>
<% @projects.each do |project| %>

<h2>Project Name: <%= link_to project.name, project_path(project), class:'name' %></h2><br><br>
<h3>Posted by: <%= link_to project.user.name, user_path(project.user) %></h3>
<% end %>